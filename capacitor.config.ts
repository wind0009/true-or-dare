import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.wind0009.actionverite',
  appName: 'Action ou Vérité',
  webDir: 'dist',
};

export default config;
